[project]
name = "slide-deck-skill"
version = "0.1.0"
description = "Locked Python environment for the slide-deck skill"
requires-python = ">=3.10"
dependencies = [
    "python-pptx>=1.0.2",
]

[dependency-groups]
dev = [
    "pyyaml>=6.0.2",
]

[tool.uv]
package = false
